import processing.core.PApplet;

public class ProcessingTest extends PApplet {	
	public void setup() {
		size(600,400);
		background(255);
		frameRate(1);
	}
	
	public void draw() {
		Disk d = new Disk(this);

		ellipse(d.x, d.y, d.rx, d.ry);
		
		fill(random(0,200),random(0,10),random(0,200));
		
	}
}