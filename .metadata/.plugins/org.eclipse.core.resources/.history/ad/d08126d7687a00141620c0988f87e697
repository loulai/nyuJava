import processing.core.PApplet;

public class ProcessingTest extends PApplet {	
	
	int timeCounter = 0;
	Disk[] totalDisks = null;
	
	public void setup() {
		
		size(600,400);
		background(255);
		frameRate(2);
		
		for(int i = 0; i < 8; i++){
			totalDisks[i] = new Disk(this);
		}
		
	}
	
	public void draw() {
		timeCounter++;
		
		if (timeCounter < 550) {
			
		}
		
	}
}